[user]
	name = {{ protonPass "pass://Work/GIT Config/name" }}
	email = {{ protonPass "pass://Work/GIT Config/email" }}
	signingkey = {{ protonPass "pass://Work/GIT Config/signingkey" }}

[core]
  sshCommand = "ssh -i ~/.ssh/id_ed25519_work.pub -F /dev/null"

[commit]
	# https://help.github.com/articles/signing-commits-using-gpg/
	gpgsign = true

[gpg]
  format = ssh

[gpg "ssh"]
  allowedSignersFile = ~/.ssh/allowed_signers
